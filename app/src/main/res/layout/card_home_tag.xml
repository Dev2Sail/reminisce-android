<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/home_tag_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/margin_large"
        android:layout_marginTop="@dimen/margin_3xlarge"
        android:fontFamily="@font/pretendard_regular"
        android:text="@string/home_tag"
        android:textSize="@dimen/title_large" />

    <com.google.android.material.chip.ChipGroup
        android:id="@+id/home_tag_chips"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/margin_regular"
        android:clipToPadding="false"
        android:paddingHorizontal="@dimen/margin_3xlarge"
        app:chipSpacing="0dp"
        app:chipSpacingVertical="-4dp"
        app:singleSelection="true">
<!-- chip은 코드 상에서 삽입 -> 몇 개나 들어갈지 어케 앎? -->
    </com.google.android.material.chip.ChipGroup>
</LinearLayout>